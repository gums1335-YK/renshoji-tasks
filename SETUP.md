# パソコン・スマホ間でタスクを共有する設定手順

このアプリは、何もしなければ今まで通り「その端末だけ」にデータが保存されます。
パソコンとスマホでデータを共有したい場合は、無料のFirebase（Google）を使って
共有をオンにできます。ログイン（メールアドレス登録など）は不要で、
「共有コード」を各端末で入力するだけで同じデータになります。

所要時間：5分程度。一度設定すれば、あとはアプリ内の「☁ 共有オフ」ボタンから
共有コードを入力するだけです。

## 1. Firebaseプロジェクトを作成する

1. https://console.firebase.google.com/ を開き、Googleアカウントでログイン
2. 「プロジェクトを追加」→ 好きな名前（例：renshoji-tasks）を入力して作成
   （Google アナリティクスは不要なのでオフのままでOK）

## 2. Firestore Database を有効化する

1. 左メニューの「構築」→「Firestore Database」を開く
2. 「データベースの作成」をクリック
3. ロケーションは任意（例：asia-northeast1 東京）
4. モードは「本番環境モード」を選択

## 3. 匿名認証を有効化する

1. 左メニューの「構築」→「Authentication」を開く
2. 「Sign-in method」タブ →「匿名」を選択して「有効にする」

これにより、メールアドレス登録なしで安全にFirestoreへ接続できます
（アプリの利用者からは何も見えず、裏側で自動的に行われます）。

## 4. Firestoreのセキュリティルールを設定する

「Firestore Database」→「ルール」タブを開き、以下に置き換えて「公開」：

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId} {
      allow read, write: if request.auth != null;
    }
  }
}
```

（匿名認証済みのアクセスのみ許可するルールです。共有コードを知らない
第三者が偶然アクセスすることを防ぐため、共有コードは他人に推測されにくい
ものにしてください。アプリの「生成」ボタンでランダムなコードを作れます。）

## 5. ウェブアプリを追加して設定情報を取得する

1. プロジェクトの概要ページ（歯車アイコン→「プロジェクトの設定」）を開く
2. 「マイアプリ」→ `</>`（ウェブ）アイコンをクリックしてアプリを登録
   （名前は任意。Firebase Hostingは不要なのでチェックしなくてOK）
3. 表示される次のようなコードをコピー：

```js
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "xxxx.firebaseapp.com",
  projectId: "xxxx",
  storageBucket: "xxxx.appspot.com",
  messagingSenderId: "...",
  appId: "..."
};
```

## 6. アプリで共有を開始する

1. アプリ右上の「☁ 共有オフ」ボタンをタップ
2. 手順5でコピーした `firebaseConfig` をそのまま貼り付け
3. 共有コードを決める（「生成」ボタンでランダムなコードを作成可）
4. 「この設定で開始する」をタップ

## 7. もう一つの端末（パソコン⇔スマホ）でも同じ手順を行う

もう一方の端末でもアプリを開き、手順6と同じ「firebaseConfig」と
「共有コード」を入力すれば、両方の端末で同じタスクデータが
リアルタイムに同期されます。

共有を解除したい場合は、「☁」ボタン→「共有を解除してこの端末だけで使う」
から解除できます（解除後もFirebase上のデータ自体は残ります）。
